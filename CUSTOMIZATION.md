# 客製化入口

請先在 Codex 選定一個專案資料夾，再進行客製化。

- `01-llm-wiki`：修改 `config.yaml` 與 Wiki page template。
- `02-schedule-assistant`：執行 `scripts/bootstrap.py`，再填寫本機 `preferences.yaml` 與 `project_types.md`。
- `03-presentation-framework`：分別修改 Theme、Layout 與當次 Content。

不要在 repository 根目錄建立共用 student profile、共用 output 或共用 Skills。

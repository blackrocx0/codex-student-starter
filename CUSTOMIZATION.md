# 學員自訂入口

請先在 Codex 開啟單一專案資料夾，再依該專案自己的 `AGENTS.md` 自訂：

1. `01-work-program`：執行適合 Windows 或 macOS 的 bootstrap，再填寫本機 `preferences.yaml` 與 `project_types.md`。
2. `02-presentation-design`：分別修改 Theme、Layout 與當次 Content。
3. `03-llm-wiki`：修改 `config.yaml` 與 Wiki page template。

只填會影響工作的必要欄位；不知道就讓 Codex 詢問，不放真實 Token、密碼、私人信箱或私人日曆 ID。
